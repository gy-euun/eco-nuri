# 🚀 에코누리 실제 운영 가이드

실제로 랜딩페이지를 운영하기 위한 단계별 가이드를 제공합니다.

## 📋 체크리스트

### 1단계: 개발 환경 확인 ✅

```bash
# 프로젝트 디렉토리로 이동
cd /c/xampp/htdocs/econuri00

# 의존성 설치 확인
npm install

# 개발 서버 실행 테스트
npm run dev
```

브라우저에서 `http://localhost:3000` 접속하여 정상 작동 확인

---

## 2단계: 보안 설정 🔒

### 2-1. 관리자 비밀번호 변경

**방법 1: 직접 해시 생성 (권장)**

```bash
# Node.js로 새 비밀번호 해시 생성
node -e "const bcrypt = require('bcryptjs'); bcrypt.hash('새비밀번호', 10).then(hash => console.log(hash));"
```

생성된 해시를 `data/admin.json` 파일의 `password` 필드에 업데이트

**방법 2: 간단한 방법**

`data/admin.json` 파일을 직접 수정:
```json
{
  "username": "admin",
  "password": "새로운_해시값",
  "createdAt": "2024-01-01T00:00:00.000Z"
}
```

### 2-2. JWT Secret 설정

`.env.local` 파일 생성 (프로젝트 루트에):

```env
JWT_SECRET=여기에_랜덤_문자열_입력
```

**랜덤 문자열 생성 방법:**
```bash
# Node.js로 랜덤 문자열 생성
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

또는 온라인 랜덤 문자열 생성기 사용

---

## 3단계: 빌드 및 테스트 🧪

### 3-1. 프로덕션 빌드

```bash
npm run build
```

빌드가 성공하면 `✓ Compiled successfully` 메시지 확인

### 3-2. 프로덕션 서버 테스트

```bash
npm start
```

`http://localhost:3000` 접속하여 정상 작동 확인

---

## 4단계: 배포 선택지 🎯

### 옵션 1: Vercel (가장 쉬움, 추천) ⭐

**장점:**
- 무료 플랜 제공
- 자동 HTTPS
- 자동 배포
- 한국 서버 지원

**배포 방법:**

1. **Vercel 계정 생성**
   - https://vercel.com 접속
   - GitHub 계정으로 로그인

2. **프로젝트 연결**
   ```bash
   # Vercel CLI 설치
   npm i -g vercel
   
   # 배포
   vercel
   ```

3. **환경 변수 설정**
   - Vercel 대시보드 → 프로젝트 → Settings → Environment Variables
   - `JWT_SECRET` 추가

4. **자동 배포 설정**
   - GitHub에 코드 푸시하면 자동 배포

**비용:** 무료 (월 100GB 대역폭)

---

### 옵션 2: 네이버 클라우드 플랫폼

**장점:**
- 한국 서버
- 저렴한 비용

**배포 방법:**

1. **서버 생성**
   - 네이버 클라우드 플랫폼 접속
   - Server > Server 생성
   - Ubuntu 22.04 LTS 선택

2. **서버 접속 및 설정**
   ```bash
   # SSH 접속
   ssh root@서버IP
   
   # Node.js 설치
   curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
   sudo apt-get install -y nodejs
   
   # PM2 설치 (프로세스 관리)
   sudo npm install -g pm2
   ```

3. **프로젝트 업로드**
   ```bash
   # Git으로 클론하거나
   git clone https://github.com/your-repo/econuri00.git
   
   # 또는 FileZilla로 파일 업로드
   ```

4. **환경 변수 설정**
   ```bash
   # .env.local 파일 생성
   nano .env.local
   # JWT_SECRET=값 입력
   ```

5. **빌드 및 실행**
   ```bash
   cd econuri00
   npm install
   npm run build
   pm2 start npm --name "econuri" -- start
   pm2 save
   pm2 startup
   ```

6. **Nginx 설정 (도메인 연결)**
   ```bash
   sudo apt install nginx
   sudo nano /etc/nginx/sites-available/default
   ```

   Nginx 설정:
   ```nginx
   server {
       listen 80;
       server_name yourdomain.com;
       
       location / {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }
   }
   ```

   ```bash
   sudo nginx -t
   sudo systemctl restart nginx
   ```

---

### 옵션 3: AWS (EC2)

**장점:**
- 안정적
- 확장 가능

**배포 방법:** 옵션 2와 유사 (서버만 AWS EC2 사용)

---

### 옵션 4: 카페24 / 가비아 등 호스팅

**장점:**
- 한국 호스팅
- 도메인 포함 패키지

**주의사항:**
- Node.js 지원 여부 확인 필요
- 일부 호스팅은 Node.js 미지원

---

## 5단계: 도메인 연결 🌐

### 5-1. 도메인 구매
- 가비아, 후이즈, 카페24 등에서 도메인 구매
- 예: `econuri.co.kr`, `econuri.kr`

### 5-2. DNS 설정

**Vercel 사용 시:**
- Vercel 대시보드 → 프로젝트 → Settings → Domains
- 도메인 추가 후 DNS 레코드 설정 안내 따르기

**자체 서버 사용 시:**
- 도메인 관리 페이지에서 A 레코드 설정
- `@` 또는 `www` → 서버 IP 주소

---

## 6단계: 운영 체크리스트 ✅

### 필수 확인 사항

- [ ] 관리자 비밀번호 변경 완료
- [ ] JWT_SECRET 환경 변수 설정 완료
- [ ] HTTPS 적용 (자동 또는 Let's Encrypt)
- [ ] 도메인 연결 완료
- [ ] 모바일에서 정상 작동 확인
- [ ] 문의 폼 제출 테스트
- [ ] 관리자 로그인 테스트
- [ ] 데이터 백업 계획 수립

### 데이터 백업

**자동 백업 스크립트 예시:**

```bash
#!/bin/bash
# backup.sh

DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/backup/econuri"
mkdir -p $BACKUP_DIR

# 데이터 폴더 백업
tar -czf $BACKUP_DIR/data_$DATE.tar.gz /path/to/econuri00/data

# 30일 이상 된 백업 삭제
find $BACKUP_DIR -name "data_*.tar.gz" -mtime +30 -delete
```

**crontab 설정 (매일 자정 백업):**
```bash
0 0 * * * /path/to/backup.sh
```

---

## 7단계: 모니터링 및 유지보수 📊

### 추천 도구

1. **Vercel Analytics** (Vercel 사용 시)
   - 트래픽 모니터링
   - 성능 분석

2. **Google Analytics**
   - 방문자 분석
   - 전환 추적

3. **Sentry** (에러 추적)
   ```bash
   npm install @sentry/nextjs
   ```

### 정기 점검

- **주간**: 문의 내역 확인
- **월간**: 데이터 백업 확인, 보안 업데이트
- **분기**: 성능 최적화, 코드 리뷰

---

## 8단계: 추가 기능 (선택사항) 🎁

### 8-1. 이메일 알림

문의 접수 시 관리자에게 이메일 발송

**추천 서비스:**
- SendGrid
- AWS SES
- 네이버 메일 API

### 8-2. SMS 알림

**추천 서비스:**
- 알리고 (aligo)
- 카카오 알림톡
- 네이버 클라우드 플랫폼 SMS

### 8-3. 데이터베이스 마이그레이션

JSON 파일 → 실제 DB (MySQL, PostgreSQL)

**장점:**
- 더 빠른 성능
- 더 안정적인 데이터 저장
- 복잡한 쿼리 가능

---

## 🆘 문제 해결

### 빌드 오류
```bash
# node_modules 삭제 후 재설치
rm -rf node_modules package-lock.json
npm install
npm run build
```

### 포트 충돌
```bash
# 다른 포트로 실행
PORT=3001 npm start
```

### 데이터 파일 권한 오류
```bash
# data 폴더 권한 설정
chmod 755 data
chmod 644 data/*.json
```

---

## 📞 지원

문제가 발생하면:
1. 브라우저 콘솔 확인 (F12)
2. 서버 로그 확인
3. 에러 메시지 확인

---

## 🎉 완료!

이제 실제로 운영할 수 있는 랜딩페이지가 준비되었습니다!

**다음 단계:**
1. 보안 설정 완료
2. 배포 플랫폼 선택
3. 도메인 연결
4. 운영 시작!
